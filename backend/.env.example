# Server
PORT=5000
NODE_ENV=development

# MongoDB - use a replica set / Atlas cluster in production for scale
MONGO_URI=mongodb://127.0.0.1:27017/learniee

# Auth
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# CORS
CLIENT_ORIGIN=http://localhost:5173
