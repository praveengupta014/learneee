# API URL - Leave empty if frontend and backend are deployed together in the same Vercel project (/api)
# Or set to your backend API URL if backend is deployed separately (e.g., https://my-backend.vercel.app or https://api.learniee.com)
VITE_API_URL=
